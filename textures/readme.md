# ONLY IMAGES

This folder should only contain images of the following extensions:
* ```.png```
* ```.jpg```

due note you aren't really required to use this folder for textures, though you must be consistent with where you put your assets and must change the ```CMakeLists.txt``` file behave how you want it to behave.

**ensure to delete this file after reading**